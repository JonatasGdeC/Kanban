namespace Kanban.App.Components.AddIcon;

public enum KanbanIcon
{
    Logo,              // 3 barras verticais da marca
    Board,             // ícone de board (grade)
    EllipsisVertical,  // 3 pontos verticais
    Eye,               // olho aberto  (mostrar senha / mostrar sidebar)
    EyeOff,            // olho fechado (esconder senha / esconder sidebar)
    Sun,               // sol (modo claro)
    Moon,              // lua (modo escuro)
    ChevronDown,       // seta para baixo
    Close,             // X (remover item)
    User,              // pessoa / perfil
    LogOut,            // seta saindo (logout)
}
