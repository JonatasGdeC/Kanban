@inherits FluxorComponent

<div class="task-card @(DragTaskState.DraggingTask?.Id == Task.Id ? "task-card--dragging" : string.Empty)"
     draggable="true"
     @ondragstart="@OnDragStart"
     @ondragend="@OnDragEnd"
     @onclick="@OpenViewTask">
    <p class="task-card-title">@Task.Name</p>
    @if (SubTaskListState.Value.SubTasks.Any())
    {
        <p class="task-card-subtasks">
            @SubTaskListState.Value.SubTasks.Count(predicate: st => st.IsDone) @DefaultLocalizer[name: "OF_LABEL"] @SubTaskListState.Value.SubTasks.Count @DefaultLocalizer[name: "SUBTASKS_COUNT"]
        </p>
    }
</div>
