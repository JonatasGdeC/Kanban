@inherits LayoutComponentBase
<MudThemeProvider/>
<MudPopoverProvider/>
<MudDialogProvider/>
<MudSnackbarProvider/>

<MudLayout>
  <NavManu OnBoardSelected="HandleBoardSelected"/>
  <MudMainContent>
    @Body
  </MudMainContent>
</MudLayout>

@code {
  private BoardsResponse? _selectedBoard;

  private void HandleBoardSelected(BoardsResponse? board)
  {
    _selectedBoard = board;
  }
}
