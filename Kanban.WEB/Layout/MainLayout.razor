@inherits LayoutComponentBase
<MudThemeProvider/>
<MudPopoverProvider/>
<MudDialogProvider/>
<MudSnackbarProvider/>

<MudLayout>
  <Header Board="@_selectedBoard"/>
  <NavManu OnBoardSelected="HandleBoardSelected"/>
  <MudMainContent>
    @Body
  </MudMainContent>
</MudLayout>

@code {
  private BoardsResponse? _selectedBoard;

  private void HandleBoardSelected(BoardsResponse? board)
  {
    _selectedBoard = board;
  }
}
